<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.movies">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.main.WatchListActivity"></activity>
        <activity android:name=".PracticeActivity" />
        <activity android:name=".ui.main.FavouriteMovieActivity" />
        <activity android:name=".ui.main.LoginActivity" />
        <activity android:name=".ui.main.UpcommingMovieActivity" />
        <activity
            android:name=".ui.main.FullscreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_fullscreen"
            android:theme="@style/FullscreenTheme" />
        <activity
            android:name=".ui.main.DetailsActivity.DetailActivity"
            android:label="@string/title_activity_upcoming_detail"
            android:theme="@style/AppTheme.Home" />
        <activity android:name=".ui.main.SearchActivity" />
        <activity android:name=".ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>